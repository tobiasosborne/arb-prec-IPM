# golden/ — arbsdp golden-master test set

Generated by bead arb-prec-IPM-b09 (7 baseline problems) and bead arb-prec-IPM-epic.3
(9 size & conditioning stress problems) using `wolframscript` (Mathematica).
All optimal values are certified by independent symbolic computation (see `gen/verify.wl`).

---

## Directory schema

Each problem lives in `golden/<name>/`:

```
golden/
  <name>/
    <name>.dat-s       -- problem in SDPA sparse format (parseable by SdpaSparse.ts)
    provenance.json    -- metadata + certified optimal value
  gen/
    generate.wl        -- Wolfram Language generator script (run once to regenerate)
    verify.wl          -- independent verification script
  verify.sh            -- shell wrapper: runs verify.wl via wolframscript
  README.md            -- this file
```

### provenance.json schema

```json
{
  "name":                 "<name>",
  "source":               "wolframscript",
  "description":          "<human-readable description>",
  "m":                    <integer, number of constraints>,
  "block_sizes":          [<integer, ...>],
  "maximize":             true,
  "optimal_value":        "<decimal string, >= 65 significant digits>",
  "optimal_value_digits": <integer, number of certified significant digits>,
  "reference_method":     "<derivation method>",
  "generated_by":         "arbsdp golden-master generator b09 / wolframscript 1.13.0"
}
```

`optimal_value` is ALWAYS a plain decimal string (no `e` notation, no spaces).
`block_sizes`: positive integer = PSD block of that size; negative integer = diagonal (LP) block.
`maximize: true` means the problem is stated as a maximization (SDPA convention).

### provenance.json schema v2 (runner fields)

v2 adds runner-facing fields consumed by the adaptive-solve regression gate. All
are OPTIONAL on read: a file without them falls back to the listed DEFAULT, so v1
files keep parsing. Calibrate the numbers from measured solver behavior
(`bench/brittle_probe.c`), never guess.

| Field | Type | Default when absent | Meaning |
|-------|------|---------------------|---------|
| `expected_status` | string | `"optimal"` | Required terminal status (`optimal`, `primal_infeasible`, `dual_infeasible`). |
| `target_digits` | int | `15` | Digits the runner REQUESTS and VERIFIES. Must be reachable within `prec_cap`; set at or just below the digits the adaptive solver actually achieves, so the correctness assertion PASSES. |
| `prec_cap` | int | `8192` | `prec_max` bits for the adaptive solve; a power of 2, set to the final_prec the problem needed (rounded up). |
| `correctness_tol_digits` | int | `0` | Slack on `target_digits` the correctness check tolerates (0 = exact reach required). |
| `expected_max_bits_per_digit` | int | `8` | GOAL ceiling on final_prec/achieved_digits (b30 target ~3-6). The current solver is far above 8 on every problem — that overrun is the `efficiency` known_gap, not a surprise. |
| `expected_max_iters` | int | `100` | Ceiling on adaptive iterations. |
| `known_gaps` | array | `[]` | Expected-fail markers; each `{kind, bead, reason}` with a CONCRETE measured reason. |

`known_gaps[].kind` vocabulary:
- `status` — the solver returns a different terminal status than `expected_status`.
- `correctness` — `target_digits` (within `correctness_tol_digits`) is NOT reached within `prec_cap`.
- `efficiency` — the bits/digit or iteration budget (`expected_max_bits_per_digit`, `expected_max_iters`) is exceeded.

xfail semantics: a `known_gap` of a given kind makes the gate TOLERATE that
failure class for this problem (expected-fail, reported but not fatal); but the
gate FAILS if that case unexpectedly PASSES, forcing removal of the now-stale
marker so the bar ratchets up.

### .dat-s format (SDPA sparse)

Parsed by `SdpaSparse.ts` (packages/solver-ipm/src/format/SdpaSparse.ts):
- Lines starting with `*` or `"` are comments.
- Line 1: `m` (number of constraints).
- Line 2: `nblocks`.
- Line 3: block sizes, space-separated.
- Line 4: `b` vector (right-hand sides), space-separated.
- Remaining lines: `i block k l v`
  - `i=0` → C (objective); `i=1..m` → A_i (constraint matrix i).
  - `block`: 1-indexed block number.
  - `k`, `l`: 1-indexed row/col, with `k <= l` (upper-triangle only).
  - `v`: value. For off-diagonal entries (`k < l`), the entry represents
    both `A_{kl}` and `A_{lk}` (full symmetric matrix), so
    `<A, X> += v * X_{kl} + v * X_{lk} = 2*v * X_{kl}`.
  - No `=` signs in data lines (the parser strips from `=` onwards as a comment).

All numeric values in the generated files are written as 65-digit plain decimals.

---

## Problems

| Name | Size | m | Optimal value | Type | Notes |
|------|------|---|---------------|------|-------|
| trivial_2x2 | 2x2 PSD | 1 | 1.0 | integer | BOUNDARY: X*=diag(1,0) singular |
| max_eigenvalue_2x2 | 2x2 PSD | 1 | 3.0 | integer | BOUNDARY: rank-1 X* |
| sdp_sqrt2 | 2x2 PSD | 2 | sqrt(2) = 1.4142... | irrational | BOUNDARY: rank-1 X* |
| lp_diagonal_block | diag block -5 | 3 | 7.0 | integer | LP as diagonal block |
| ill_conditioned_3x3 | 3x3 PSD | 3 | 1+10^-6 | rational | mildly ill-conditioned Schur |
| mixed_blocks | 2x2 PSD + scalar | 2 | 1+sqrt(2)/2 = 1.7071... | irrational | mixed PSD+LP blocks |
| max_eig_tridiag_3x3 | 3x3 PSD | 1 | 2+sqrt(3) = 3.7320... | irrational | BOUNDARY: rank-1 X* |
| max_eig_path_4 | 4x4 PSD | 1 | 2+2cos(pi/5) = 3.6180... | irrational | BOUNDARY: rank-1 X*; eigenvalue gap 1.00; prec_cap 512 |
| max_eig_path_8 | 8x8 PSD | 1 | 2+2cos(pi/9) = 3.8793... | irrational | BOUNDARY: rank-1 X*; eigenvalue gap 0.347; prec_cap 1024 |
| max_eig_path_16 | 16x16 PSD | 1 | 2+2cos(pi/17) = 3.9659... | irrational | BOUNDARY: rank-1 X*; eigenvalue gap 0.101; prec_cap 2048 |
| disparate_scale_sqrt2 | 2x2 PSD | 2 | sqrt(2) = 1.4142... | irrational | Schur cond ~4e32; RHS span P/Q=2e16; prec_cap 2048 |
| diag_weight_kappa6 | 3x3 PSD | 1 | 1.0 | integer | data cond(C)=1e6; BOUNDARY: X*=e1e1^T; prec_cap 256 |
| diag_weight_kappa10 | 3x3 PSD | 1 | 1.0 | integer | data cond(C)=1e10; BOUNDARY: X*=e1e1^T; prec_cap 256 |
| diag_weight_kappa12 | 4x4 PSD | 1 | 1.0 | integer | data cond(C)=1e12; BOUNDARY: X*=e1e1^T; prec_cap 256 |
| two_block_corr_coupled | 2x2+2x2 PSD | 5 | 2*sqrt(2) = 2.8284... | irrational | 2 coupled PSD blocks; both rank-1 boundary; prec_cap 2048 |
| separable_12block | 12x(2x2 PSD) | 12 | 80.5708... (sum of 12 surds) | irrational | 12 PSD blocks; m=12 Schur assembly; prec_cap 512 |

### Detailed descriptions

**trivial_2x2** — Minimal boundary case: `max x_11 s.t. x_11=1, X 2x2 PSD`.
The optimal X*=diag(1,0) is rank-deficient (singular). Tests CLAUDE.md invariant 1
(certify from dual side; verified Cholesky of X* will fail).

**max_eigenvalue_2x2** — `max <A,X> s.t. tr(X)=1, X 2x2 PSD` where A=[[2,1],[1,2]].
Variational formula: opt = lambda_max(A) = 3. X* = [[1/2,1/2],[1/2,1/2]] is rank-1 singular.

**sdp_sqrt2** — `max x_12 s.t. x_11=1, x_22=2, X 2x2 PSD`.
PSD requires x_12^2 <= 2; opt = sqrt(2). X* = [[1,sqrt(2)],[sqrt(2),2]] is rank-1.
Tests high-precision irrational value and boundary optimum simultaneously.

**lp_diagonal_block** — `max x1+2*x2 s.t. x1+x2<=4, x1<=3, x2<=3` with slacks.
Encoded as a single diagonal block (block_size=-5). Tests LP-as-diagonal-block code path.

**ill_conditioned_3x3** — `max x_11+1e-6*x_22 s.t. x_11=1, x_22=1, 2*x_12=0, X 3x3 PSD`.
The small coefficient epsilon=1e-6 in C creates mild ill-conditioning in the Schur complement
matrix during the IPM solve. Tests precision handling for nearly-flat objectives.

**mixed_blocks** — `max (X_1)_11 + x_2 s.t. tr(X_1)=1, x_2+x_12=1/2` with
block_sizes=[2,-1]. Tests the mixed-block code path with one PSD and one LP block.

**max_eig_tridiag_3x3** — `max <A,X> s.t. tr(X)=1, X 3x3 PSD` where A=[[3,1,0],[1,2,1],[0,1,1]].
opt = lambda_max(A) = 2+sqrt(3). Tests a 3x3 problem with irrational optimum and rank-1
boundary X*.

---

### epic.3 — size & conditioning stress family (9 additional problems)

Bead arb-prec-IPM-epic.3 adds 9 problems covering four holes not addressed by the baseline 7:
(1) scalable PSD block size (n=4,8,16); (2) Schur conditioning >=1e10 and data conditioning
ladder 1e6/1e10/1e12; (3) two or more genuine coupled PSD blocks; (4) m>=12 Schur assembly.
All 9 have analytic optima (closed form or exact integer) verified in mpmath 1.3.0 and
sympy 1.14.0 against independent cross-checks. All are boundary optima (rank-1 X*).
Provenance v2 runner fields are calibrated from measured solver behavior per the schema.

**max_eig_path_4** — `max <C,X> s.t. tr(X)=1, X 4x4 PSD` with
C = T_4(2,1) (symmetric tridiagonal: diagonal 2, off-diagonal 1).
opt = lambda_max(C) = 2+2cos(pi/5) = 3.6180... (irrational; closed form; verified mpmath and
sympy). X* is the rank-1 projector onto the top eigenvector (boundary optimum, exercises
CLAUDE.md invariant 1). Eigenvalue gap = 1.00; svec dim = 10. Size-stress baseline for n=4;
measured: final_prec 512, 13 matched digits, 37 bits/digit, NUM at prec 128/256 (trajectory
128:NUM:3.3 256:NUM:7.3 512:OPT). Efficiency known_gap (bead epic.6).

**max_eig_path_8** — same family at n=8.
opt = 2+2cos(pi/9) = 3.8793... Eigenvalue gap = 0.347; svec dim = 36.
Measured: final_prec 1024, 15 matched digits, 68 bits/digit, NUM at prec 128-512 (trajectory
128:NUM:-2.1 256:NUM:4.7 512:NUM:8.7 1024:OPT). Efficiency known_gap (bead epic.6).
The growing svec dimension and shrinking eigenvalue gap make this the first problem at "n>=8 REQUIRED".

**max_eig_path_16** — same family at n=16.
opt = 2+2cos(pi/17) = 3.9659... Eigenvalue gap = 0.101; svec dim = 136.
Measured: final_prec 2048, 15 matched digits, 138 bits/digit, NUM at prec 128-1024 (trajectory
128:NUM:-3.0 256:NUM:-2.4 512:NUM:2.4 1024:NUM:9.1 2048:OPT). Efficiency known_gap (bead
epic.6). This is the "n>=16 REQUIRED" problem most likely to expose precision starvation
(CLAUDE.md rule 6: "works at n=3, breaks at n=16").

**disparate_scale_sqrt2** — `max x_12 s.t. x_11=2e8, x_22=1e-8, X 2x2 PSD`.
PSD constraint gives x_12^2 <= x_11*x_22 = 2, so opt = sqrt(P*Q) = sqrt(2). X* is rank-1
(det = 0). This is the Schur-conditioning stress problem: the NT scaling W ~ diag(P,Q) near
the boundary gives Schur cond(M) ~ (P/Q)^2 ~ 4e32 >> 1e10 throughout the central path (not
just at the boundary). Also stresses disparate-magnitude RHS: b = [2e8, 1e-8], span P/Q=2e16.
Measured: final_prec 2048, 13 matched digits, 153 bits/digit, 20 iters at final prec (trajectory
128:NUM:-1.9 256:NUM:4.2 512:NUM:7.5 1024:NUM:10.7 2048:OPT). Efficiency known_gap (bead epic.6).

**diag_weight_kappa6** — `max <diag(1,1e-3,1e-6),X> s.t. tr(X)=1, X 3x3 PSD`.
opt = max weight = 1 (exact). X* = e1*e1^T, rank-1 boundary. Data-matrix cond(C) = 1e6
(1e6 rung of the conditioning ladder). Measured: final_prec 256, 14 matched digits,
18 bits/digit (trajectory 128:NUM:7.8 256:OPT). Efficiency known_gap (bead epic.6).

**diag_weight_kappa10** — `max <diag(1,1e-5,1e-10),X> s.t. tr(X)=1, X 3x3 PSD`.
opt = 1 (exact). Data-matrix cond(C) = 1e10 (meets the kappa>=1e10 bar independently).
Same calibrated behavior as kappa6 (final_prec 256, 14 matched digits, 18 bits/digit).
The 1e10-rung sibling for trend analysis across the conditioning ladder.

**diag_weight_kappa12** — `max <diag(1,1e-4,1e-8,1e-12),X> s.t. tr(X)=1, X 4x4 PSD`.
opt = 1 (exact). Data-matrix cond(C) = max/min weight = 1e12 exactly (same conditioning
class as H_8 Hilbert moment matrix, cond ~1.53e10, but with a provable exact optimum — the
reason this construction replaces Hilbert as a kappa>=1e10 stress source). Measured:
final_prec 256, 14 matched digits, 18 bits/digit (trajectory 128:NUM:7.8 256:OPT).
Required kappa>=1e10 representative. Efficiency known_gap (bead epic.6).

**two_block_corr_coupled** — two 2x2 PSD blocks X1, X2, coupled.
`max x1_12 + x2_12 s.t. x1_11=1, x1_22=2, x2_11=1, x2_22=2, x1_12-x2_12=0`.
Each block: t^2 <= 1*2 = 2, so t <= sqrt(2); coupling forces both off-diagonals equal;
opt = 2*sqrt(2) = 2.8284... BOTH blocks sit at their rank-1 boundary simultaneously (det=0
for both X1* and X2*). The coupling constraint (5th row of the 5x5 Schur) mixes the two
blocks so the Schur is NOT block-diagonal. Stresses multi-block Schur assembly and per-block
NT scaling with two simultaneous boundary blocks. Measured: final_prec 2048, 16 matched
digits, 131 bits/digit (trajectory 128:NUM:0.8 256:NUM:1.8 512:NUM:6.9 1024:NUM:7.4 2048:OPT).
Efficiency known_gap (bead epic.6).

**separable_12block** — twelve 2x2 PSD blocks (m=12).
For c=1..12, block c has cost A_c=[[c,1],[1,0]] and trace constraint tr(X_c)=1. Separable:
opt = sum_{c=1}^{12} lambda_max(A_c) = sum_{c=1}^{12}(c/2 + sqrt(c^2/4+1)) = 80.5708...
(sum of 12 surds; verified mpmath closed form vs per-block numeric eigensolve, diff 0 at 40
digits). block_sizes = [2]*12. Each X_c* is the rank-1 projector onto its top eigenvector
(boundary). Schur is 12x12 but block-diagonal (one 1x1 block per constraint — the problem
is separable). Stresses m=12 Schur ASSEMBLY and bookkeeping with 12 PSD blocks, not dense
ill-conditioned factorization (that role is disparate_scale_sqrt2). Measured: final_prec 512,
16 matched digits, 33 bits/digit (trajectory 128:NUM:3.7 256:NUM:7.7 512:OPT).
Efficiency known_gap (bead epic.6).

---

## How beads b10 and b23 consume this set

**b10 (solver integration test):** For each problem, run `arbsdp solve <name>.dat-s --prec P`
and check that the returned `[lb, ub]` bracket CONTAINS `optimal_value` (read from
`provenance.json`). This is the "bracket" relation from PRD §6; it is falsifiable (any
failure is a P0 bug per CLAUDE.md rule 2). The check is:
```
lb <= optimal_value <= ub   (all parsed as high-precision numbers)
```

**b23 (beat/bracket regression suite):** For each problem, also check the "beat" relation:
that the solver returns more correct digits than a float64 solver would. Concretely, compare
`|midpoint([lb,ub]) - optimal_value|` against the float64 solver's error on the same problem.

---

## Regenerating

```bash
cd /path/to/arb-prec-IPM
wolframscript -file golden/gen/generate.wl
```

The generator is deterministic and idempotent (overwrites existing files).

## Verifying

```bash
bash golden/verify.sh
```

Expected output: `ALL PASS: stored values match independent computations to stated digits.`
